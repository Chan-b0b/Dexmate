"""Hardware-validated drivers, reused as-is from case_battery_demo.

Do not rewrite these; they talk to real hardware (suction HTTP, Cognex telnet,
Robotiq Modbus) and are tuned. Only the ``config`` import path was adjusted.
"""
