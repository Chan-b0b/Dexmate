# Local labeling + training environment (your GPU box / laptop).
# NOT needed here: dexcontrol / the robot stack — that's only for capture.py,
# which runs on the Orin. Copy the captured data/ over from the robot instead.
#
# Order matters:
#   1. install torch FIRST for your CUDA:  https://pytorch.org/get-started/
#         (SAM2 needs torch >= 2.5.1)
#   2. pip install -r requirements.txt
#   3. pip install "git+https://github.com/facebookresearch/sam2.git"
#   4. download the checkpoint (see README "Local setup")

numpy
opencv-python
matplotlib
ultralytics>=8.1     # YOLO-OBB + detect (train + inference)
